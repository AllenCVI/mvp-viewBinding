package com.newblink.blink.android.common;

public class MakeStrUtil {

    private static final String sv="GcSsrsv";
    private static final String fd= "aqeloy";
    private static final String bg ="vnsltwY";
    private static final String sdv="fslkdjvc4zi";
    private static final String gw = "uT5Kddsbba";
    private static final String ht = "sfdnjskv1";

    public final static String getKey(){
        return fd.substring(fd.length()-1)+bg.substring(bg.length()-2)+sdv.substring(sdv.length()-3)
                +sv.substring(1,5)+gw.substring(0,5)+ht.substring(ht.length()-1);
    }

}
